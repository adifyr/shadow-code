{
  "name": "shadow-code-ai",
  "displayName": "shadow-code-ai",
  "description": "AI-powered pseudo-code to code conversion engine.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.108.0"
  },
  "categories": [
    "Programming Languages",
    "AI"
  ],
  "activationEvents": [
    "workspaceContains:**/*.shadow"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "shadowCodeAI.openInShadowMode",
        "title": "Open In Shadow Mode",
        "icon": "$(mirror)"
      },
      {
        "command": "shadowCodeAI.copyCode",
        "title": "Copy Over Existing Code",
        "icon": "$(copy)"
      }
    ],
    "languages": [
      {
        "id": "shadow",
        "extensions": [
          ".shadow"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "shadow",
        "scopeName": "source.shadow",
        "path": "./syntaxes/shadow.tmLanguage.json"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "shadowCodeAI.openInShadowMode",
          "when": "resourceScheme == file && resourceExtname != .shadow",
          "group": "navigation"
        },
        {
          "command": "shadowCodeAI.copyCode",
          "when": "resourceExtname == .shadow",
          "group": "navigation"
        }
      ],
      "editor/title/context": [
        {
          "command": "shadowCodeAI.openInShadowMode",
          "when": "resourceScheme == file && resourceExtname != .shadow",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Shadow Code AI",
      "properties": {
        "shadowCodeAI.apiKey": {
          "type": "string",
          "title": "OpenRouter API Key",
          "default": "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",
          "description": "Your OpenRouter API key.",
          "order": 1
        },
        "shadowCodeAI.model": {
          "type": "string",
          "title": "AI Model",
          "default": "openai/gpt-oss-120b:free",
          "enum": [
            "openai/gpt-oss-120b:free"
          ],
          "enumDescriptions": [
            "An open-weight, 117B-parameter Mixture-of-Experts (MoE) language model from OpenAI designed for high-reasoning, agentic, and general-purpose production use cases."
          ],
          "description": "The AI model to use for code generation.",
          "order": 2
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.108.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.1",
    "eslint": "^9.39.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  },
  "dependencies": {
    "@google/genai": "^1.35.0",
    "@openrouter/sdk": "^0.4.0"
  }
}