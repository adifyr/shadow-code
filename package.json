{
  "name": "shadow-code-ai",
  "displayName": "Shadow Code AI",
  "description": "A novel approach to coding with AI.",
  "version": "0.1.8",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "Shadow Code AI requires a trusted workspace to function."
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/adifyr/shadow-code-ai.git"
  },
  "engines": {
    "vscode": "^1.108.0"
  },
  "categories": [
    "Programming Languages",
    "AI"
  ],
  "activationEvents": [
    "workspaceContains:**/*.shadow"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "icons": {
      "shadow-mode": {
        "description": "Main Icon for enabling Shadow Mode.",
        "default": {
          "fontPath": "assets/shadow-icon.woff",
          "fontCharacter": "\\f000"
        }
      }
    },
    "commands": [
      {
        "command": "shadowCodeAI.openInShadowMode",
        "title": "Open In Shadow Mode",
        "icon": "$(shadow-mode)"
      },
      {
        "command": "shadowCodeAI.copyCode",
        "title": "Copy Over Existing Code",
        "icon": "$(copy)"
      },
      {
        "command": "shadowCodeAI.generateCode",
        "title": "Generate Code",
        "icon": "$(sparkle)"
      }
    ],
    "languages": [
      {
        "id": "shadow",
        "aliases": [
          "Shadow"
        ],
        "extensions": [
          ".shadow"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "shadow",
        "scopeName": "source.shadow",
        "path": "./syntaxes/shadow.tmLanguage.json"
      }
    ],
    "keybindings": [
      {
        "command": "shadowCodeAI.generateCode",
        "key": "ctrl+alt+enter",
        "mac": "cmd+alt+enter",
        "when": "resourceExtname == .shadow || editorLangId == shadow"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "shadowCodeAI.openInShadowMode",
          "when": "resourceScheme == file && resourceExtname != .shadow",
          "group": "navigation"
        },
        {
          "command": "shadowCodeAI.copyCode",
          "when": "resourceExtname == .shadow",
          "group": "navigation"
        },
        {
          "command": "shadowCodeAI.generateCode",
          "when": "resourceExtname == .shadow",
          "group": "navigation"
        }
      ],
      "editor/title/context": [
        {
          "command": "shadowCodeAI.openInShadowMode",
          "when": "resourceScheme == file && resourceExtname != .shadow",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Shadow Code AI",
      "properties": {
        "shadowCodeAI.apiKey": {
          "type": "string",
          "title": "Gemini API Key",
          "default": "REMOVED",
          "description": "Your Gemini API key.",
          "order": 1
        },
        "shadowCodeAI.model": {
          "type": "string",
          "title": "AI Model",
          "default": "gemini-flash-lite-latest",
          "enum": [
            "gemini-flash-lite-latest",
            "gemini-flash-latest"
          ],
          "description": "The AI model to use for code generation.",
          "order": 2
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.108.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.1",
    "eslint": "^9.39.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  },
  "dependencies": {
    "@google/genai": "^1.38.0",
    "@openrouter/sdk": "^0.4.0",
    "diff": "^8.0.3"
  }
}